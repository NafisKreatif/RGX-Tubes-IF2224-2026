program DynamicMatrixAccess;

type
  Matrix == array[1 .. 2] of array[1 .. 3] of integer;

var
  grid: Matrix;
  row, col: integer;

begin
  row := 2;
  col := 3;
  grid[row, col] := 42;
  writeln('Matrix value = ', grid[2, 3])
end.
